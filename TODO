-- GATEKEEPER

user --new
	-> read config
	-> load user index
	-> does user exist in index?
			no: -> generate password
			    -> make pw md5 hash
			    -> render template
			    -> encrypt password
			    -> save encrypted password in password store
			    -> save user file hash in index
			yes:
				-> nope... can't do this

